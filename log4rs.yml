refresh_rate: 30 seconds

appenders:
    stdout:
        kind: console

root:
    level: warn # we don't care about tracing from serenity
    appenders:
        - stdout

loggers:
    intersection:
        level: debug
